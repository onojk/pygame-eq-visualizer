# archive/originals_consolidated_code/

The original `consolidated_code/` directory, untouched.

It contained two files (`entropic_visualizer.py`, `experiment1.py`) — neither was actually
unique, so nothing from here needed to be rescued into the live tree.

| File                       | Status                                                                    |
|----------------------------|---------------------------------------------------------------------------|
| `entropic_visualizer.py`   | Identical to the misnamed top-level `entropic_visualizer.py` (basic-viz duplicate, MD5 d2cdaec7...). The **real** entropic visualizer was in `originals_backups_minimal/_files/entropic_visualizer.py` and is now in `visualizers/entropic/`. |
| `experiment1.py`           | Identical to the top-level `experiment1.py` (now in `originals_top_level/`). |
